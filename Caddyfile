{
  acme_dns cloudflare {env.CLOUDFLARE_API_TOKEN}
  
  # Import additional global options
  import /conf/Caddyfile.d/global/*.caddy
}

# Then import site definitions separately
import /conf/Caddyfile.d/sites/*.caddy